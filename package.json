{
  "name": "personal-websitetools",
  "version": "1.0.0",
  "description": "Personal WebsiteTools — landing page, perforated panel designer, and utilities",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
